name: Build and Deploy to VPS

on:
  push:
    branches:
      - main          # Production deployment
      - production    # Production deployment
      - development   # Test/Dev deployment
      - dev           # Test/Dev deployment
  workflow_dispatch: # Allow manual trigger

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ========================
  # Build and Push Docker Image
  # ========================
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-name-lower: ${{ steps.lower.outputs.repository }}
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Convert repository name to lowercase
        id: lower
        run: |
          echo "repository=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.lower.outputs.repository }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  # ========================
  # Deploy to Test VPS (development branch)
  # ========================
  deploy-test:
    name: Deploy to Test VPS
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/development' || github.ref == 'refs/heads/dev'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Test VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_TAG: ${{ github.sha }}
          REPO_LOWER: ${{ needs.build.outputs.image-name-lower }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          envs: IMAGE_TAG,REPO_LOWER
          script: |
            set -e
            
            echo "🚀 Starting TEST deployment from development branch..."
            
            cd /opt/rispo || { echo "❌ Directory /opt/rispo not found"; exit 1; }
            
            echo "📥 Pulling latest code from development..."
            git fetch origin
            git checkout development
            git pull origin development
            
            echo "🔐 Logging in to GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            echo "🐳 Pulling Docker image..."
            docker pull ghcr.io/${REPO_LOWER}:development || \
            docker pull ghcr.io/${REPO_LOWER}:latest
            
            echo "🛑 Stopping old container..."
            docker compose -f docker-compose.test.yml down app || true
            
            echo "🔄 Starting new container..."
            docker compose -f docker-compose.test.yml up -d --force-recreate app
            
            echo "⏳ Waiting for health checks..."
            sleep 15
            
            echo "✅ TEST deployment completed!"
            docker compose -f docker-compose.test.yml ps
            git pull origin development
            
            echo "🔐 Logging in to GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            echo "🐳 Pulling Docker image..."
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:development || \
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            
            echo "🛑 Stopping old container..."
            docker compose -f docker-compose.test.yml down app || true
            
            echo "🔄 Starting new container..."
            docker compose -f docker-compose.test.yml up -d --force-recreate app
            
            echo "⏳ Waiting for health checks..."
            sleep 15
            
            echo "✅ TEST deployment completed!"
            docker compose -f docker-compose.test.yml ps

  # ========================
  # Deploy to Production VPS (main branch)
  # ========================
  deploy-prod:
    name: Deploy to Production VPS
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/production'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_TAG: ${{ github.sha }}
          REPO_LOWER: ${{ needs.build.outputs.image-name-lower }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          envs: IMAGE_TAG,REPO_LOWER
          script: |
            set -e
            
            echo "🚀 Starting deployment..."
            
            # Navigate to application directory
            cd /opt/rispo || { echo "❌ Directory /opt/rispo not found"; exit 1; }
            
            # Pull latest changes
            echo "📥 Pulling latest code..."
            git pull origin main
            
            # Log in to GitHub Container Registry
            echo "🔐 Logging in to GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            # Pull latest Docker image
            echo "🐳 Pulling Docker image..."
            docker pull ghcr.io/${REPO_LOWER}:latest
            
            # Update environment file with new image tag
            sed -i "s/IMAGE_TAG=.*/IMAGE_TAG=${IMAGE_TAG}/" .env.prod
            
            # Stop and remove old containers
            echo "🛑 Stopping old containers..."
            docker compose -f docker-compose.prod.yml down app-1 app-2 || true
            
            # Start new containers with zero-downtime deployment
            echo "🔄 Starting new containers..."
            docker compose -f docker-compose.prod.yml up -d --no-deps --force-recreate app-1 app-2
            
            # Wait for health checks
            echo "⏳ Waiting for health checks..."
            sleep 15
            
            # Verify deployment
            echo "✅ Verifying deployment..."
            if docker compose -f docker-compose.prod.yml ps | grep -q "unhealthy"; then
              echo "❌ Health check failed! Rolling back..."
              docker compose -f docker-compose.prod.yml logs app-1 app-2
              exit 1
            fi
            
            # Cleanup old images
            echo "🧹 Cleaning up old images..."
            docker iPRODUCTION deployment completed successfully!"
            
            # Display running containers
            docker compose -f docker-compose.prod.yml ps

      - name: Production Deployment notification
        if: always()
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: |
            Production Deployment: ${{ job.status }}
            Commit: ${{ github.sha }}
            Branch: ${{ github.ref }}
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
        continue-on-error: true

  # ========================
  # Database Backup (runs after successful production deployment)
  # ========================
  backup:
    name: Backup Database
    runs-on: ubuntu-latest
    needs: deploy-prod
    if: success()
    
    steps:
      - name: Trigger Database Backup
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            cd /opt/rispo
            ./scripts/backup.sh
            echo "✅ Backup completed!"
            echo "✅ Backup completed!"
