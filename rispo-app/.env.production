# Backend API URL for production/mobile builds
REACT_APP_API_URL=http://135.125.133.211/api

# For local network testing (phone and PC on same WiFi):
# Find your PC's local IP with: ipconfig (Windows) or ifconfig (Linux)
# Then use: http://192.168.x.x:8080/api
